/**
 * The centre slot in the tab bar.
 *
 * A route only because a tab needs one: pressing the button opens the compose
 * sheet from the layout and the press is cancelled, so this screen is never
 * actually shown. It exists to hold the slot in the bar.
 */
export default function ComposeSlot() {
  return null
}
