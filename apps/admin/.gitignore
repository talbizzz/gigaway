# dependencies
node_modules/

# build output
dist/

# local env files — .env.example is the only one tracked. Vite loads plain
# .env in every mode; deploy-admin.yml never uses a file, it exports
# VITE_* as real environment variables before building.
.env
.env.local
.env.*.local

# macOS
.DS_Store

# typescript
*.tsbuildinfo
